import pyhdf 
from pyhdf.SD import SD, SDC
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.colors as colors
from PIL import Image

def f(x):
    return (np.cos(x))

plt.figure
plt.plot(np.arange(0,10,0.05),np.fft.fftshift( np.abs(np.fft.fft(f(np.arange(0,10,0.05))))**2 ))
plt.show()

#so this works as I expect. Now i will  try 2D 