import matplotlib.pyplot as plt
import numpy as np
import matplotlib.colors as colors
from PIL import Image

#creating two slits
shape=np.zeros((100,100, 3), dtype=np.uint8)
color = (255, 255, 255)
shape[50, 40] = color
shape[50, 60] = color

#plt.figure()
#plt.imshow(shape)
#plt.show()
print(shape)
#getting fourier amplitudes
ft=np.fft.fft2(shape)
ft=np.fft.fftshift(ft)
spectrum=(np.fft.fftfreq(100, d=10e-6))

plt.imsave('C:\Internship\Internship-\Investigating fft v2\dots.png', shape)

#i like what the countour plot is doing, but i think i am plotting it wrong. It seems to just plot amplitudes as x and z, rather 
#than ploting agains k 

plt.figure()
plt.contour([(spectrum), (spectrum)], (np.abs(ft))**2, levels=1000) 
plt.colorbar()
plt.show()

