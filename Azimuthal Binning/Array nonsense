import numpy as np 

p=np.array([[1,2],[2,5],[1,8]])

#print(p[:,0])
boolian=(np.any(p==1, axis=1))#, where=489))
print(boolian)

#def Binning(m):
#    m_mod=m
#    amps=np.array([])
#    k_vals=np.unique(m[:,0]) 
#    for i in range(0,len(k_vals)):
#        Test=k_vals[i]
#        Temp_store=np.array([])
#        for j in range(0,10):
#            while m_mod[:,0][j] == Test:
#                Temp_store=np.append(Temp_store, m_mod[:,1][j])
#            else:
#                Amps=np.append(Amps, np.sum(Temp_store))
#                m_mod=np.delete(m, [np.arange(0, len(Temp_store))], axis=0) 
#        return (k_vals, amps)

#print(Binning(Tot))